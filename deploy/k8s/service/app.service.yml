apiVersion: v1
kind: Service
metadata:
    name: backend-deployment-test-service
spec:
    ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: 80
    selector:
        app: backend-deployment-test
